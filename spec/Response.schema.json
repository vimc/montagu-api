{
    "id": "Response",
    "type": "object",
    "properties": {
        "status": { 
            "enum": [ "success", "failure" ] 
        },
        "data": {},
        "errors": {
            "type": "array",
            "items": { "$ref": "Error.schema.json" },
            "minimumLength": 0
        }
    },
    "required": ["status", "errors", "data" ]
}