{
    "id": "Response",
    "type": "object",
    "properties": {
        "status": { 
            "enum": [ "success", "failure" ] 
        },
        "data": {},
        "errors": {
            "type": "array",
            "items": { "$ref": "#/definitions/error" },
            "minimumLength": 0
        }
    },
    "required": ["status", "errors", "data" ],
    "definitions": {
        "error": {
            "id": "Error",
            "properties": {
                "code": { "type": "string" },
                "message": { "type": "string" }
            },
            "required": [ "code", "message" ]
        }
    }
}