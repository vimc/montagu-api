package org.vaccineimpact.api.tests.resumable

import org.vaccineimpact.api.app.models.ResumableInfo
import org.vaccineimpact.api.test_helpers.MontaguTests

class ResumableInfoTests: MontaguTests() {

    fun `upload is finished if whole file is smaller than minimum chunk`() {
        val sut = ResumableInfo()

    }

}