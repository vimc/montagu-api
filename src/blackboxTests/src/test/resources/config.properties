db.host=${db_host}
db.port=${db_port}
db.name=${db_name}
db.username=${db_username}
db.password=${db_password}

testdb.template_name=${testdb_template_name}

app.url=${app_url}

flow.url=${flow_url}
packit.api.url=${packit_api_url}
